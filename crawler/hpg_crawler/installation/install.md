Install/upgrade chrome driver with:
- `$ brew cask install chromedriver` or
- `$ brew cask reinstall chromedriver`

Or:
- apt-get install chromium-chromedriver


Install xvfb:
- `$ apt install -y xvfb`

Install pyvirtualdisplay:
- `$ pip3 install pyvirtualdisplay`

